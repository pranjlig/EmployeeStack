<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>
    <% if (success && success.length) {%> 
        <div class="alert alert-success alert-dismissible fade show alert-div" role="alert">
          <p class="alert-p"><%- success -%> </p>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        <% }%> 
    <% if (error && error.length) {%> 
        <div class="alert alert-danger alert-dismissible fade show alert-div" role="alert">
            <p class="alert-p"><%- error -%> </p>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% }%>
    <h4>Total Expense: <%- project.total %> </h4>
    <h5><%- project.terminationDate ? `Termination Date: ${project.terminationDate.getFullYear()}-${("0" + (project.terminationDate.getMonth() + 1)).slice(-2)}-${("0" + project.terminationDate.getDate()).slice(-2)}` : ""%></h5>
    
    <form action="", method="POST">
        <div>
            <label for="label">Name: </label>
            <input type="text" id="label" name="projectName" value="<%- project.name %>" required>
        </div>
        <!-- <div>
            <label for="status">Status: </label>
            <input type="text" id="status" name="status" value="<%- project.status %>" required readonly>
        </div> -->
        <label for="">Status: <%- project.status %></label>
    
        <!-- <select name="status" id="status" required>
            <option value="In-Progress" <%- project.status === "In-Progress" ? "selected" : "" %> >In-Progress</option>
            <option value="Completed" <%- project.status === "Completed" ? "selected" : "" %>>Completed</option>            
        </select> -->
        <div>
            <label for="startDate">Start Date: </label>
            <input type="date" id="startDate" name="startDate" required value=<%- `${project.startDate.getFullYear()}-${("0" + (project.startDate.getMonth() + 1)).slice(-2)}-${("0" + project.startDate.getDate()).slice(-2)}` %> >
        </div>
        <div>
            <label for="requiredEndDate">Required Date</label>
            <input type="date" name="requiredEndDate" id="requiredEndDate" value=<%- project.requiredEndDate ? `${project.requiredEndDate.getFullYear()}-${("0" + (project.requiredEndDate.getMonth() + 1)).slice(-2)}-${("0" + project.requiredEndDate.getDate()).slice(-2)}` : ""%> >
        </div>
        <div>
            <textarea name="description" id="string" cols="30" rows="2" ><%= project.description %></textarea>
        </div>
        <button>Submit</button>
    </form>
    <form action=<%- `/admin/projects/${project._id}?_method=delete` %>  method="POST">
        <button>Delete</button>
    </form>
    <form action=<%- `/admin/projects/${project._id}/terminate` %>  method="POST">
        <button>Terminate</button>
    </form>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>
    
</body>
</html>